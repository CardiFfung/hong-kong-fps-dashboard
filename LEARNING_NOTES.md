# 由零理解呢個作品

呢份筆記按實作次序讀。先打開 Dashboard 玩一次，再逐段睇程式，唔需要一次背晒。

## 1. 官方數據究竟係乜？

API 即係程式向官方系統索取資料嘅入口。JSON 係有欄名、有層次嘅文字格式，Python 可以將佢讀成字典同清單。

打開 `data/current.json`，入面 snapshot 指向今次成功版本；再打開 `data/raw/<snapshot>/volume.json`。
每個下載頁保留 `request_url` 同完整 `payload`。`header.success` 必須為 true；資料喺 `result.records`，`datasize` 表示呢頁有幾多行。

`pagesize=1000` 係每頁上限，`offset=1000` 即跳過頭 1,000 行。今次每組只有 96 行，一頁已夠；程式照樣支援更多頁。如果剛好滿一頁，會再讀下一頁直到不足一頁，包括最後空頁。

**小練習：** 搵 2026-08 嘅 total。筆數應為 82,587,554，金額原值為 806,269,783.54935 千港元。

## 2. 點樣清理同合併？

DataFrame 係 Python 入面有欄名嘅資料表，好似試算表；每行代表一個月。merge 係用共同 key 對齊兩張表，呢度 key 係月份。

睇 `src/pipeline.py` 嘅 `clean()` 同 `prepare()`：

1. 檢查欄位齊唔齊、月份格式有冇錯、同一月份有冇重複。
2. 數值轉成數字；文字、負數、無限大、非整數筆數會令更新失敗。
3. 外部合併（outer merge）保留兩邊所有月份。只喺一邊存在嘅月份會報告，唔會靜靜丟失。
4. 重新建立完整月份序列；冇數據嘅月留空，唔當成零。
5. 三層合計逐月核對；不一致會記錄原始差額，唔修改官方數字。

原始數值空白可以保留，因為「未知」唔等於「冇交易」。重複月份則無法確定應揀邊行，所以拒絕。

groupby 係將同一組嘅資料放埋一齊再計總數或平均；呢份資料已經一月一行，唔需要為咗用呢個功能而再加總。尤其 total 同子類別一齊加，會重複計算。

**小練習：** 睇 `test_missing_month_not_previous_row_and_exact_yoy()`，如果二月冇資料，三月可唔可以用一月計「月增長」？答案係唔可以。

## 3. 四個重要計算

所有數據計算集中喺 `derive()`；`app.py` 只負責畫圖、格式同單位縮放。

| 指標 | 公式 | 解讀 |
|---|---|---|
| 港元金額 | 官方千港元 × 1,000 | 唔可以將千港元原值當港元 |
| 平均每筆 | 同月同類別港元金額 ÷ 筆數 | 筆數為零或缺失時不適用 |
| MoM | (本月 − 上月) ÷ 上月 × 100% | 同上月比较 |
| YoY | (本月 − 上年同月) ÷ 上年同月 × 100% | 同上年同月比较 |

50,000 千港元 × 1,000 ÷ 10,000 筆 = 5,000 港元／筆。
如果上月 100 筆、本月 120 筆，絕對變化 +20 筆，百分比 +20%。如果上月係零，絕對變化仍可計，百分比就不適用。

完整月份序列建立後，`shift(12)` 先真正代表上年同月。2018-09 可能只包含推出日，唔參與增長比較，因此 2018-10 MoM、2019-09 YoY 都唔會用呢個部分月份做基準。

12 個月移動平均要有連續 12 個合資格數值先顯示。平均每筆圖嘅移動平均係 12 個「月平均」嘅簡單平均，並非全年總金額除全年總筆數，兩者權重唔同。

**小練習：** 用計數機驗證 2026-08 筆數 MoM：(82,587,554 − 84,300,073) ÷ 84,300,073 × 100 ≈ −2.03%。

## 4. 點讀每張圖？

| 圖 | 應該點讀 | 唔可以推斷 |
|---|---|---|
| 每月筆數 | 相同類別每月發生幾多筆，虛線幫手睇長期方向 | 唔等於獨立用戶或新客人數 |
| 每月金額 | 流經系統金額規模，單位係億港元 | 唔等於消費總值、收入、盈利 |
| 即時／批量比例 | 分母係同月 total；兩條線分別佔幾多 | 唔係成長率，亦唔係全港支付市場份額 |
| 平均每筆 | 同月金額除筆數 | 唔係中位數、典型用戶消費額 |
| 即時轉帳子類別 | 代碼、帳號、其他三類互相比較；表格分母係 rtctp | 唔知道每位用戶偏好，唔保證收款人係個人 |

收款人代碼發起嘅個人轉帳佔比有兩個問題要分清：

- 佔所有即時轉帳：代碼 ÷ rtctp；2026-08 筆數約 41.70%。
- 只喺兩種個人發起方式內比較：代碼 ÷（代碼 + 帳號）；呢個另存於 `proxy_volume_share_personal_pct`。

**小練習：** 將類別轉為批量支付，比較平均每筆同總支付平均。可以描述差異，唔好憑空解釋背後用戶身份。

## 5. API 壞咗點算？

`refresh()` 必須兩組資料都成功、通過基本驗證，先更新 current 指標。原始檔有 SHA-256 校驗值，可以偵測儲存後嘅內容改動，但唔等於數位簽署或獨立官方認證。

有舊快照：顯示紅色失敗訊息同擷取日期，再顯示舊數據。冇快照：停止畫圖，唔產生示範數字。平常啟動亦標明係快照，唔會扮即時數據。

**小練習：** 睇 `tests/test_app.py` 嘅 API 失敗測試；佢用模擬錯誤，唔需要真係關掉全屋網絡。

## 6. 點樣自己再做一次？

喺 project 資料夾啟動 Terminal，按 README 建立環境，再執行：

```bash
python -m src.pipeline
python -m src.pipeline --offline
python -m pytest -q
python -m streamlit run app.py
```

第一行需要網絡；第二行由已存官方回應重新計算，可離線核對。測試需要先安裝 `requirements-dev.txt`。

下一個練習：加入「即時扣帳」嘅一句自動發現。先喺 pipeline 寫計算／敘述，再喺 app 顯示，唔好將公式散落喺每張圖。

## 7. 面試時點證明自己識？

唔好只講用了幾多套件。示範一個指標點由官方 JSON 追到表格、公式、圖表，解釋點解要分清分母，再主動講限制。
呢份初稿由 AI 協助建立；投履歷前應親手重跑、修改一項功能，並能夠逐步解釋。唔好聲稱已用於銀行生產環境或令業務成長。

10 分鐘講解大綱見 `docs/INTERVIEW_OUTLINE.md`。
